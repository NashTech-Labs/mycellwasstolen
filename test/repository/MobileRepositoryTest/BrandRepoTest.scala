/*package repository.MobileRepositoryTest

import play.api.test.FakeApplication
import play.api.test.Helpers._
import utils.Connection
import org.scalatest.FunSuite
import org.scalatest.BeforeAndAfterEach
import model.repository.Brand
import model.repository.{ BrandRepository, MobileRepository, ModelRepository }
import model._
import java.util.Date
import model.repository.SlickDbDriver
import org.scalatest.BeforeAndAfterAll

class BrandRepoTest extends FunSuite with BeforeAndAfterAll {

  // Set up database schema
  /*  test("insert brand") {
    running(FakeApplication()) {
      val insertedMobile = BrandRepository.insertBrand(Brand("nokia",(new Date()).toString(),))
      assert(insertedMobile === Right(Some(1)))
    }
  }*/

}*/
